<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pending Blogs - Blogify</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .blog-img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 6px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
 
  <div class="container-fluid">
    <div class="row">
      <!-- Sidebar -->
      <aside class="col-md-2 bg-light p-3 min-vh-100">
        <%- include('partials/sidebar') %>
      </aside>

      <!-- Main Content -->
      <main class="col-md-10 p-5">
        <h2 class="mb-4">⏳ Pending Blog Approvals</h2>

        <% if (blogs.length === 0) { %>
          <p>No pending blogs.</p>
        <% } else { %>
          <div class="row">
            <% blogs.forEach(blog => { %>
              <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm">
                  <img src="<%= blog.image %>" alt="Blog Image" class="blog-img card-img-top">

                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= blog.title %></h5>
                    <p class="card-text"><%= blog.description.substring(0, 120) %>...</p>
                    <p class="card-text">
                      <small class="text-muted"><strong>By:</strong> <%= blog.User?.name || 'Unknown' %></small><br>
                      <small class="text-muted"><strong>Sector:</strong> <%= blog.Sector?.name || 'N/A' %></small>
                    </p>

                    <div class="mt-auto">
                      <a href="/blogs/blogs/<%= blog.id %>" class="btn btn-sm btn-outline-primary me-2">📖 Read More</a>

                      <form action="/blogs/approve/<%= blog.id %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-sm btn-success">✅ Approve</button>
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            <% }) %>
          </div>
        <% } %>
      </main>
    </div>
  </div>

  <!-- Bootstrap JS CDN -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
