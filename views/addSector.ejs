<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Sector - Blogify</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
 
  <div class="container-fluid">
    <div class="row">
      <!-- Sidebar -->
      <aside class="col-md-2 bg-light p-3 min-vh-100">
        <%- include('partials/sidebar') %>
      </aside>

      <!-- Main Content -->
      <main class="col-md-10 py-5 px-4">
        <h2 class="mb-4">➕ Add New Sector</h2>

        <div class="card shadow-sm">
          <div class="card-body">
            <form action="/blogs/add-sector" method="POST">
              <div class="mb-3">
                <label for="name" class="form-label">Sector Name</label>
                <input type="text" id="name" name="name" class="form-control" placeholder="Enter sector name" required>
              </div>

              <button type="submit" class="btn btn-primary">Add Sector</button>
            </form>
          </div>
        </div>
      </main>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
